import React from "react";
import PlaceholderPage from "../placeholder";

const Render = () => (
  <PlaceholderPage
    title="Render"
    description="Queue and monitor your renders in real-time with full control."
  />
);

export default Render;
