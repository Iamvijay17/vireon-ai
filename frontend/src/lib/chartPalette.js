// Categorical palette for chart rows that don't already carry semantic
// status meaning (templates, resolutions, categories, ...) - cycled by
// index so an arbitrary-length breakdown still reads as distinct colors.
export const CATEGORICAL_PALETTE = [
  "var(--color-accent-500)",
  "var(--color-info-500)",
  "var(--color-success-500)",
  "var(--color-warning-500)",
  "var(--color-danger-500)",
];
